# Individual contribution report

## Thông tin

- Họ và tên: Lâm Hải Dương
- Mã học viên: 2A202602676
- Nhóm: sloppers
- Repository/branch: ToRong31/K4-L3B-RAG-Pipeline / main

## Phần việc đã thực hiện

| Module/deliverable | Việc tôi trực tiếp làm | File/commit/PR | Trạng thái |
|---|---|---|---|
| **Task 1 — Thu thập tài liệu pháp lý** | Thu thập và thẩm định 5 văn bản PDF chính thức về tuyển sinh VinUni (3 bản gốc và bổ sung 2 văn bản biểu phí, đào tạo từ cổng chính sách VinUni) | `src/task1_collect_legal_docs.py`, `data/landing/legal/` / Commit `70ace45` | Done |
| **Task 2 — Crawl bài viết tin tức** | Viết hàm crawl trích xuất 5 bài báo chính thống tuyển sinh VinUni kèm 4 trường metadata bắt buộc (`url`, `title`, `date_crawled`, `content_markdown`) | `src/task2_crawl_news.py`, `data/landing/news/*.json` / Commit `70ace45` | Done |
| **Task 3 — Chuẩn hóa Markdown** | Hiện thực MarkItDown chuyển PDF và JSON thành 8 file Markdown chuẩn hóa (độ dài 5.000 - 75.000 ký tự, vượt chuẩn ≥ 200 ký tự) | `src/task3_convert_markdown.py`, `data/standardized/` / Commit `70ace45` | Done |
| **Task 4 — Chunking & Indexing** | Chia 396 chunks bằng `RecursiveCharacterTextSplitter` (chunk_size=500, overlap=50), embed bằng `keepitreal/vietnamese-sbert` (dim 768) và upsert vào ChromaDB collection `rag_documents` | `src/task4_chunking_indexing.py`, `chroma_db/` / Commit `70ace45` | Done |
| **Tài liệu bàn giao** | Soạn thảo tài liệu bàn giao chi tiết về dữ liệu, embedding model, dimension, chunk size và hướng dẫn dùng chung corpus cho Trọng | `DATA_HANDOVER.md` / Commit `70ace45` | Done |

Chỉ kê khai công việc có thể đối chiếu bằng file, commit, pull request, test hoặc kết quả evaluation.

## Quyết định kỹ thuật quan trọng

Mô tả tối đa hai quyết định mà bạn trực tiếp tham gia:

1. **Quyết định:** Sử dụng mô hình embedding local `keepitreal/vietnamese-sbert` (dimension 768) làm mặc định cho pipeline thay vì phụ thuộc API ngoài hay model nặng 2.2GB (`BAAI/bge-m3`).  
   **Lý do/evidence:** Mô hình tối ưu chuyên biệt cho ngữ nghĩa tiếng Việt, tốc độ mã hóa cực nhanh (embed toàn bộ 396 chunks chỉ mất ~76 giây), hoạt động ổn định và hỗ trợ chạy demo offline mượt mà.  
   **Trade-off:** Kích thước vector (768) nhỏ hơn BGE-M3 (1024), nhưng đổi lại tốc độ inference và khả năng tái lập môi trường không gặp rủi ro nghẽn mạng hay giới hạn token API.

2. **Quyết định:** Bổ sung văn bản chính sách số trực tiếp từ cổng chính sách VinUni (`policy.vinuni.edu.vn`) và giữ nguyên cấu trúc phân cấp (Mục lục, Điều, Khoản, Bảng biểu) khi chuyển đổi sang Markdown.  
   **Lý do/evidence:** Hai file PDF scan ban đầu không có digital text (MarkItDown trả về 0 ký tự). Việc bổ sung văn bản chính sách số giúp vượt qua bài kiểm thử acceptance (độ dài ≥ 200 ký tự) và cung cấp cơ sở dữ liệu chi tiết cho việc trích dẫn (citation) của LLM.  
   **Trade-off:** Các trang mục lục đầu có các ký tự chấm `...` từ bản gốc PDF, tuy nhiên phần lớn các chunk nội dung phía sau được giữ trọn vẹn ngữ cảnh.

## Kiểm thử và kết quả

- **Test hoặc query tôi đã dùng:** `pytest tests/test_acceptance.py -q` và `pytest tests/test_contracts.py -k "chunk" -q`.
- **Kết quả trước/sau nếu có:** Trước khi làm, 12 test bị fail (toàn bộ các test data và chunking fail do thiếu dữ liệu và chưa có hàm). Sau khi hoàn thành, 4/4 bài test thuộc phạm vi Data & Chunking đều PASSED (`test_corpus_has_required_legal_documents`, `test_corpus_has_required_news_with_metadata`, `test_standardized_output_covers_both_source_types`, `test_chunk_documents_preserves_identity_and_metadata`).
- **Lỗi đã phát hiện và cách xử lý:** Phát hiện lỗi UnicodeEncodeError trên Windows console (`cp1252`) khi in ký tự tiếng Việt, đã cấu hình lại `sys.stdout.reconfigure(encoding="utf-8")` trong các task script để sửa triệt để.

## Điều còn hạn chế

- **Một hạn chế cụ thể của phần tôi làm:** Hiện chưa có module OCR tự động cho các tài liệu PDF scan dạng ảnh thuần cũ nếu gặp tài liệu scan không có lớp văn bản điện tử.
- **Nếu có thêm thời gian, thay đổi đầu tiên tôi sẽ thực hiện:** Tích hợp thư viện OCR quang học (như Surya hoặc Tesseract OCR) để tự động nhận dạng ký tự từ các file PDF scan ảnh trước khi chuyển sang Markdown.

## Xác nhận đóng góp

Tôi xác nhận nội dung trên phản ánh đúng phần việc của mình và có thể giải thích hoặc chạy lại trong buổi demo.

- Ngày: 25/09/2026
- Tên thành viên: Lâm Hải Dương
